# pnpm workspace topology.
# Dependencies are NOT installed in the foundation phase — this file declares
# the package topology so Phase 2 can `pnpm install` and wire Vite + Tauri.
packages:
  - "ui/app"
  - "ui/packages/*"
