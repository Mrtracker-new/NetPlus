[package]
name = "netpulse-ai"
description = "AI Explanation Service — THE ONLY crate permitted outbound network egress."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# NOTE: this is the single egress boundary. A CI check asserts no
# OTHER crate pulls an outbound-network dependency. Networking deps belong here
# and nowhere else.
[dependencies]
netpulse-core = { path = "../netpulse-core" }
netpulse-storage = { path = "../netpulse-storage" }

[dev-dependencies]
toml = "1.1"

[lints]
workspace = true
